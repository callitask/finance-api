# /**
#  * AI-CONTEXT:
#  *
#  * Purpose:
#  * - Core infrastructure definition for the Treishvaam backend ecosystem.
#  *
#  * Scope:
#  * - Manages internal networking, service discovery, and container lifecycles.
#  *
#  * Critical Dependencies:
#  * - Backend: relies on treishvaam-db, treishvaam-redis, elasticsearch, rabbitmq, keycloak, and permission-fixer.
#  * - RabbitMQ: Must be healthy (not just started) for backend to boot successfully.
#  *
#  * Security Constraints:
#  * - Keycloak, Grafana, and RabbitMQ UIs must remain on loopback or private networks, never exposed on 0.0.0.0.
#  * - Database credentials must be mapped dynamically via host env variables.
#  *
#  * IMMUTABLE CHANGE HISTORY (DO NOT DELETE):
#  * - EDITED (L4-ADA Canary Cryptographic Seeds):
#  * • Mapped `WG_PRIVATE_KEY_SEED` and `CANARY_SECRET_KEY` from Infisical to the `aegis-canary-server` environment.
#  *
#  * - EDITED (Enterprise Wazuh HIDS & Zero-Trust Canary Bridge):
#  * • Deployed `wazuh-manager` service to natively accept `wazuh-agent` enrollments, securing the host.
#  *
#  * - EDITED (Canary Vendor Paradox & Envoy Distroless Fix):
#  * • Migrated the `printenv > .env` volatile bridge to the `entrypoint:` directive in `aegis-canary-server` so it evaluates before the vendor application boots. This safely bypasses the vendor's hardcoded physical file requirement using ephemeral container memory.
#  * • Removed `healthcheck` from `envoy-sidecar`. The proxy is functioning perfectly, but the distroless image lacks the `nc` shell utility, causing Docker to falsely label the healthy proxy as unhealthy.
#  *
#  * - EDITED (Canary Hidden File Globbing Resolution):
#  * • Changed the volatile memory bridge from `printenv > .env` to `printenv > frontend.env` in `aegis-canary-server`. The vendor's Python parser strictly uses `*.env` globbing, which natively ignores hidden Linux files.
#  *
#  * - EDITED (Canary Strict Parser Fix):
#  * • Replaced `printenv` with explicit `echo` mappings wrapped in escaped quotes (`\"$$VAR\"`) inside the `aegis-canary-server` entrypoint. This guarantees compliance with Python's strict `dotenv` parsing engine and protects cryptographic strings from shell mangling.
#  *
#  * - EDITED (Wazuh Indexer Bridge):
#  * • Bridged `wazuh-manager` to the existing `treishvaam-elastic` cluster by injecting `INDEXER_URL` and `ELASTICSEARCH_URL`. This satisfies Filebeat's dependency requirement, preventing infinite connection loops and restoring the container's healthcheck state without requiring a dedicated wazuh.indexer node.
#  *
#  * - EDITED (Canary Python 2.7 & Wazuh Clusterd Stabilization):
#  * • Removed double quotes from Canary `echo` mappings. Python 2.7's legacy `dotenv` parser includes quotes in the string value, causing cryptographic key validation to fail natively. Outputting pure, unquoted strings to `canary.env` satisfies the legacy dependency.
#  * • Removed the incompatible Elastic 8.17 bridge (Wazuh 4.14 requires OpenSearch/Wazuh Indexer). 
#  * • Injected `hostname: wazuh.manager` into the `wazuh-manager` service. The `wazuh-clusterd` daemon was crashing because it requires a canonical hostname to bind its internal node configuration, which caused the container healthcheck to fail.
#  *
#  * - DO-NOT-DELETE RULE (ABSOLUTE):
#  * This IMMUTABLE CHANGE HISTORY section acts as the institutional memory for future AI sessions. 
#  * It must never be deleted, truncated, rewritten, or regenerated. Future AI must append only. 
#  */

networks:
  treish_net:
    driver: bridge

volumes:
  app_logs:
  app_uploads:
  app_sitemaps:

services:
  treishvaam-db:
    image: mariadb:10.6
    container_name: treishvaam-db
    restart: always
    networks:
      - treish_net
    environment:
      MYSQL_ROOT_PASSWORD: ${PROD_DB_PASSWORD}
      MYSQL_DATABASE: finance_db
      MARIADB_ENCRYPTION_KEY: ${MARIADB_ENCRYPTION_KEY}
    entrypoint:
      - /bin/sh
      - -c
      - |
        mkdir -p /etc/mysql/encryption
        CLEAN_KEY=$$(echo "$$MARIADB_ENCRYPTION_KEY" | grep -oE '[A-Fa-f0-9]{64}')
        printf '1;%s\n' "$$CLEAN_KEY" > /etc/mysql/encryption/keyfile
        chown mysql:mysql /etc/mysql/encryption/keyfile
        exec docker-entrypoint.sh mariadbd --log-bin=mariadb-bin --server-id=1 --binlog-format=ROW
    volumes:
      - ./data/mariadb:/var/lib/mysql
      - ./config/mariadb/encryption.cnf:/etc/mysql/conf.d/encryption.cnf:ro
    deploy:
      resources:
        limits:
          memory: 1024M
    healthcheck:
      test: [ "CMD-SHELL", "mysqladmin ping -h localhost -u root --password=$$MYSQL_ROOT_PASSWORD || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 10

  keycloak-db:
    image: mariadb:10.6
    container_name: treishvaam-keycloak-db
    restart: always
    networks:
      - treish_net
    environment:
      MYSQL_ROOT_PASSWORD: ${PROD_DB_PASSWORD}
      MYSQL_DATABASE: keycloak
      MYSQL_USER: keycloak
      MYSQL_PASSWORD: ${KEYCLOAK_DB_PASSWORD}
    volumes:
      - ./data/keycloak-db:/var/lib/mysql
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: [ "CMD-SHELL", "mysqladmin ping -h localhost -u root --password=$$MYSQL_ROOT_PASSWORD || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 10

  keycloak:
    image: quay.io/keycloak/keycloak:25.0.0
    container_name: treishvaam-keycloak
    restart: always
    networks:
      - treish_net
    environment:
      KC_DB: mariadb
      KC_DB_URL: jdbc:mariadb://keycloak-db/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: ${KEYCLOAK_DB_PASSWORD}
      KC_HTTP_RELATIVE_PATH: /auth
      KC_HOSTNAME: backend.treishvaamgroup.com
      KC_HOSTNAME_STRICT: "false"
      KEYCLOAK_ADMIN: keycloak-admin
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
      KC_PROXY_HEADERS: xforwarded
      KC_HTTP_ENABLED: "true"
      KC_HEALTH_ENABLED: "true"
      KC_METRICS_ENABLED: "true"
      FINANCE_FRONTEND_URL: ${FINANCE_FRONTEND_URL:-https://treishvaamfinance.com}
      FINANCE_FRONTEND_LOCAL_URL: ${FINANCE_FRONTEND_LOCAL_URL:-http://localhost:3000}
    command: [ "start-dev", "--import-realm" ]
    volumes:
      - ./config/keycloak/realm-export.json:/opt/keycloak/data/import/realm.json
    deploy:
      resources:
        limits:
          memory: 768M
    depends_on:
      keycloak-db:
        condition: service_healthy

  treishvaam-redis:
    image: redis:7-alpine
    container_name: treishvaam-redis
    restart: always
    networks:
      - treish_net
    volumes:
      - ./data/redis:/data
    command:
      - redis-server
      - --requirepass
      - ${REDIS_PASSWORD}
      - --save
      - "60"
      - "1"
      - --loglevel
      - warning
      - --stop-writes-on-bgsave-error
      - "no"
      - --protected-mode
      - "yes"
      - --bind
      - 0.0.0.0
      - --rename-command
      - FLUSHALL
      - ""
      - --rename-command
      - FLUSHDB
      - ""
      - --rename-command
      - CONFIG
      - ""
      - --rename-command
      - DEBUG
      - ""
      - --rename-command
      - MONITOR
      - ""
    environment:
      REDISCLI_AUTH: ${REDIS_PASSWORD}
    deploy:
      resources:
        limits:
          memory: 256M
    healthcheck:
      test: [ "CMD-SHELL", "redis-cli -a $$REDISCLI_AUTH ping | grep PONG" ]
      interval: 5s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: aegis-canary-redis
    restart: always
    networks:
      - treish_net
    deploy:
      resources:
        limits:
          memory: 128M
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5

  aegis-canary-server:
    image: thinkst/canarytokens:v2_latest
    container_name: aegis-canary-server
    restart: always
    entrypoint:
      - sh
      - -c
      - |
        echo WG_PRIVATE_KEY_SEED=$${WG_PRIVATE_KEY_SEED} > canary.env
        echo CANARY_SECRET_KEY=$${CANARY_SECRET_KEY} >> canary.env
        echo CANARY_DOMAINS=$${CANARY_DOMAINS} >> canary.env
        echo CANARY_PUBLIC_DOMAIN=$${CANARY_PUBLIC_DOMAIN} >> canary.env
        echo CANARY_NXDOMAINS=$${CANARY_NXDOMAINS} >> canary.env
        exec twistd -noy frontend.tac
    networks:
      - treish_net
    environment:
      CANARY_DOMAINS: "canary.treishvaamgroup.com"
      CANARY_PUBLIC_DOMAIN: "canary.treishvaamgroup.com"
      CANARY_NXDOMAINS: "nx.treishvaamgroup.com"
      WG_PRIVATE_KEY_SEED: ${WG_PRIVATE_KEY_SEED}
      CANARY_SECRET_KEY: ${CANARY_SECRET_KEY}
    deploy:
      resources:
        limits:
          memory: 256M
    depends_on:
      redis:
        condition: service_healthy

  minio:
    image: minio/minio
    container_name: treishvaam-minio
    restart: always
    networks:
      - treish_net
    environment:
      MINIO_ROOT_USER: admin
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    volumes:
      - ./data/minio:/data
    command: [ "server", "/data", "--console-address", ":9001" ]
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:9000/minio/health/live" ]
      interval: 30s
      timeout: 20s
      retries: 3

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.17.0
    container_name: treishvaam-elastic
    restart: always
    networks:
      - treish_net
    environment:
      - discovery.type=single-node
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
      - xpack.security.enabled=true
      - ELASTIC_PASSWORD=${ELASTIC_PASSWORD}
      - xpack.security.enrollment.enabled=false
      - xpack.security.http.ssl.enabled=false
      - xpack.security.transport.ssl.enabled=false
    volumes:
      - ./data/elastic:/usr/share/elasticsearch/data
    deploy:
      resources:
        limits:
          memory: 768M
    healthcheck:
      test: [ "CMD-SHELL", "curl -u elastic:$$ELASTIC_PASSWORD -s http://localhost:9200/_cluster/health | grep -E -q 'status.*(green|yellow)'" ]
      interval: 20s
      timeout: 10s
      retries: 10

  tunnel:
    image: cloudflare/cloudflared
    container_name: treishvaam-tunnel
    restart: always
    networks:
      - treish_net
    command: [ "tunnel", "run" ]
    environment:
      - TUNNEL_TOKEN=${CLOUDFLARE_TUNNEL_TOKEN}
    deploy:
      resources:
        limits:
          memory: 128M

  envoy-sidecar:
    image: envoyproxy/envoy:v1.29-latest
    container_name: treishvaam-envoy
    restart: always
    networks:
      - treish_net
    volumes:
      - ./config/envoy.yaml:/etc/envoy/envoy.yaml
    deploy:
      resources:
        limits:
          memory: 256M
    depends_on:
      - backend

  aegis-zkp-service:
    build: ./aegis/zkp-service
    image: treishvaam/aegis-zkp-service:latest
    container_name: aegis-zkp-service
    restart: always
    networks:
      - treish_net
    ports:
      - "127.0.0.1:9090:9090"
    deploy:
      resources:
        limits:
          memory: 256M
    healthcheck:
      test: ["CMD", "nc", "-z", "127.0.0.1", "9090"]
      interval: 15s
      timeout: 5s
      retries: 5

  wazuh-manager:
    image: wazuh/wazuh-manager:4.14.5
    container_name: treishvaam-wazuh-manager
    hostname: wazuh.manager
    restart: always
    networks:
      - treish_net
    deploy:
      resources:
        limits:
          memory: 1024M
    healthcheck:
      test: ["CMD", "/var/ossec/bin/wazuh-control", "status"]
      interval: 15s
      timeout: 5s
      retries: 5

  wazuh-agent:
    image: wazuh/wazuh-agent:4.14.5
    container_name: wazuh-agent
    restart: always
    networks:
      - treish_net
    privileged: true
    pid: host
    environment:
      - WAZUH_MANAGER_SERVER=wazuh-manager
    deploy:
      resources:
        limits:
          memory: 128M
    depends_on:
      wazuh-manager:
        condition: service_healthy

  permission-fixer:
    image: alpine:latest
    container_name: treishvaam-permission-fixer
    networks:
      - treish_net
    user: "root"
    volumes:
      - app_logs:/workspace/logs
      - app_uploads:/workspace/uploads
      - app_sitemaps:/workspace/sitemaps
    command: [ "sh", "-c", "mkdir -p /workspace/logs /workspace/uploads /workspace/sitemaps && chown -R 1000:1000 /workspace/logs /workspace/uploads /workspace/sitemaps && chmod -R 777 /workspace/logs /workspace/uploads /workspace/sitemaps" ]

  backend:
    build:
      context: .
      args:
        - CACHE_BUST=${CACHE_BUST:-1}
    image: ghcr.io/callitask/finance-api:latest
    restart: always
    networks:
      - treish_net
    environment:
      - PROD_DB_URL=${PROD_DB_URL}
      - PROD_DB_USERNAME=${PROD_DB_USERNAME}
      - PROD_DB_PASSWORD=${PROD_DB_PASSWORD}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - INTERNAL_API_SECRET_KEY=${INTERNAL_API_SECRET_KEY}
      - CONTENT_SIGNING_KEY=${CONTENT_SIGNING_KEY}
      - APP_ADMIN_EMAIL=${APP_ADMIN_EMAIL}
      - APP_ADMIN_PASSWORD=${APP_ADMIN_PASSWORD}
      - MARKET_DATA_API_KEY=${MARKET_DATA_API_KEY}
      - ALPHAVANTAGE_API_KEY=${ALPHAVANTAGE_API_KEY}
      - FINNHUB_API_KEY=${FINNHUB_API_KEY}
      - NEWS_API_KEY=${NEWS_API_KEY}
      - GA4_PROPERTY_ID=${GA4_PROPERTY_ID}
      - GA4_BIGQUERY_PROJECT_ID=${GA4_BIGQUERY_PROJECT_ID}
      - GA4_BIGQUERY_DATASET_ID=${GA4_BIGQUERY_DATASET_ID}
      - MINIO_ACCESS_KEY=admin
      - MINIO_SECRET_KEY=${MINIO_ROOT_PASSWORD}
      - CLOUDFLARE_ACCOUNT_ID=${CLOUDFLARE_ACCOUNT_ID}
      - CLOUDFLARE_THREAT_KV_NAMESPACE_ID=${CLOUDFLARE_THREAT_KV_NAMESPACE_ID}
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN}
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_DATA_REDIS_HOST=treishvaam-redis
      - SPRING_DATA_REDIS_PASSWORD=${REDIS_PASSWORD}
      - SPRING_REDIS_HOST=treishvaam-redis
      - SPRING_REDIS_PASSWORD=${REDIS_PASSWORD}
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - SPRING_ELASTICSEARCH_URIS=http://elasticsearch:9200
      - SPRING_ELASTICSEARCH_USERNAME=elastic
      - SPRING_ELASTICSEARCH_PASSWORD=${ELASTIC_PASSWORD}
      - STORAGE_S3_ENDPOINT=http://minio:9000
      - SPRING_RABBITMQ_HOST=rabbitmq
      - SPRING_RABBITMQ_USERNAME=${RABBITMQ_DEFAULT_USER}
      - SPRING_RABBITMQ_PASSWORD=${RABBITMQ_DEFAULT_PASS}
      - APP_PYTHON_SCRIPT_PATH=/app/scripts/market_data_updater.py
      - MANAGEMENT_ZIPKIN_TRACING_ENDPOINT=http://tempo:9411/api/v2/spans
      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI=https://backend.treishvaamgroup.com/auth/realms/treishvaam
      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI=http://keycloak:8080/auth/realms/treishvaam/protocol/openid-connect/certs
      - LOGGING_FILE_NAME=/app/logs/application-${HOSTNAME}.log
      - LINKEDIN_TOKEN_ENCRYPTION_KEY=${LINKEDIN_TOKEN_ENCRYPTION_KEY}
      - USER_EMAIL_ENCRYPTION_KEY=${USER_EMAIL_ENCRYPTION_KEY}
      - CONTACT_EMAIL_ENCRYPTION_KEY=${CONTACT_EMAIL_ENCRYPTION_KEY}
      - CONTACT_MESSAGE_ENCRYPTION_KEY=${CONTACT_MESSAGE_ENCRYPTION_KEY}
      - AUDIT_IP_ENCRYPTION_KEY=${AUDIT_IP_ENCRYPTION_KEY}
      - AEGIS_DB_SIGNING_KEY=${AEGIS_DB_SIGNING_KEY}
      - AEGIS_EDGE_SECRET=${AEGIS_EDGE_SECRET}
      - JAVA_TOOL_OPTIONS=-Xmx768m -Xms512m
    volumes:
      - ./backend-app.war:/app/app.war
      - ./ga4-credentials.json:/app/ga4-credentials.json
      - ./scripts:/app/scripts
      - app_uploads:/app/uploads
      - app_sitemaps:/app/sitemaps
      - app_logs:/app/logs
    devices:
      - /dev/random:/dev/random
      - /dev/urandom:/dev/urandom
    deploy:
      mode: replicated
      replicas: 2
      resources:
        limits:
          memory: 1024M
    depends_on:
      treishvaam-db:
        condition: service_healthy
      treishvaam-redis:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      tempo:
        condition: service_started
      keycloak:
        condition: service_started
      permission-fixer:
        condition: service_completed_successfully
    healthcheck:
      test: [ "CMD-SHELL", "python3 -c 'import socket; s = socket.socket(socket.AF_INET, socket.SOCK_STREAM); s.settimeout(1); s.connect((\"localhost\", 8080)); s.close();' || exit 1" ]
      interval: 15s
      timeout: 10s
      retries: 15
      start_period: 160s

  nginx:
    image: openresty/openresty:alpine
    container_name: treishvaam-nginx
    restart: always
    networks:
      - treish_net
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/lua:/etc/nginx/lua:ro
      - app_uploads:/app/uploads
      - app_sitemaps:/app/sitemaps
    deploy:
      resources:
        limits:
          memory: 128M
    depends_on:
      backend:
        condition: service_healthy
      keycloak:
        condition: service_started
    healthcheck:
      test: [ "CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost/robots.txt || exit 1" ]
      interval: 15s
      timeout: 5s
      retries: 3

  loki:
    image: grafana/loki:2.9.2
    container_name: treishvaam-loki
    restart: always
    networks:
      - treish_net
    volumes:
      - ./config/loki-config.yml:/etc/loki/local-config.yaml
    command: [ "-config.file=/etc/loki/local-config.yaml" ]
    deploy:
      resources:
        limits:
          memory: 256M
    healthcheck:
      test: [ "CMD", "wget", "--quiet", "--tries=1", "--output-document=-", "http://localhost:3100/ready" ]
      interval: 10s
      timeout: 5s
      retries: 5

  promtail:
    image: grafana/promtail:2.9.2
    container_name: treishvaam-promtail
    restart: always
    networks:
      - treish_net
    volumes:
      - ./config/promtail-config.yml:/etc/promtail/config.yml
      - app_logs:/app/logs
    command: [ "-config.file=/etc/promtail/config.yml" ]
    deploy:
      resources:
        limits:
          memory: 128M

  prometheus:
    image: prom/prometheus:latest
    container_name: treishvaam-prometheus
    restart: always
    networks:
      - treish_net
    volumes:
      - ./config/prometheus.yml:/etc/prometheus/prometheus.yml
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: [ "CMD", "wget", "--quiet", "--tries=1", "--output-document=-", "http://localhost:9090/-/healthy" ]
      interval: 10s
      timeout: 5s
      retries: 5

  grafana:
    image: grafana/grafana:latest
    container_name: treishvaam-grafana
    restart: always
    networks:
      - treish_net
    ports:
      - "127.0.0.1:3001:3000"
    user: "0"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - ./data/grafana:/var/lib/grafana
      - ./config/grafana-datasources.yml:/etc/grafana/provisioning/datasources/datasources.yml
      - ./config/grafana-dashboards.yml:/etc/grafana/provisioning/dashboards/dashboards.yml
      - ./dashboards:/var/lib/grafana/dashboards
      - ./config/grafana-alerting.yml:/etc/grafana/provisioning/alerting/alerts.yml
    deploy:
      resources:
        limits:
          memory: 384M
    depends_on:
      - prometheus
      - loki
      - tempo

  tempo:
    image: grafana/tempo:latest
    container_name: treishvaam-tempo
    restart: always
    networks:
      - treish_net
    command: [ "-config.file=/etc/tempo.yaml" ]
    volumes:
      - ./config/tempo.yaml:/etc/tempo.yaml
      - ./data/tempo:/tmp/tempo
    deploy:
      resources:
        limits:
          memory: 256M

  rabbitmq:
    image: rabbitmq:3.12-management
    container_name: treishvaam-rabbitmq
    restart: always
    networks:
      - treish_net
    ports:
      - "127.0.0.1:15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS}
      RABBITMQ_SERVER_DEFINITION_FILE: /etc/rabbitmq/definitions.json
    volumes:
      - ./config/rabbitmq/definitions.json:/etc/rabbitmq/definitions.json:ro
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "check_running"]
      interval: 10s
      timeout: 10s
      retries: 10

  backup-service:
    build: ./backup
    container_name: treishvaam-backup
    restart: always
    networks:
      - treish_net
    environment:
      DB_HOST: treishvaam-db
      DB_USER: root
      DB_PASS: ${PROD_DB_PASSWORD}
      DB_NAME: finance_db
      S3_ENDPOINT: http://minio:9000
      S3_BUCKET: treishvaam-backups
      MINIO_ACCESS_KEY: ${BACKUP_MINIO_ACCESS_KEY}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD}
      BACKUP_ENCRYPTION_KEY: ${BACKUP_ENCRYPTION_KEY}
    deploy:
      resources:
        limits:
          memory: 128M
    depends_on:
      treishvaam-db:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "ps", "aux"]
      interval: 30s
      timeout: 10s
      retries: 3