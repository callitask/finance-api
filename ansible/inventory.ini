# ==============================================================================
# AI-CONTEXT:
#
# Purpose:
#   - Static inventory registration map for the Treishvaam Group infrastructure.
#   - Defines environment routing boundaries for Zero-State Ignition (ZSI) plays.
#
# Scope:
#   - Allocates host endpoints, control interfaces, and SSH configuration bounds.
#   - Must NEVER house vault secrets or inline execution tokens.
#
# Critical Dependencies:
#   - ansible/setup-server.yml: Consumes these host definitions for deployment targeting.
#
# Security Constraints:
#   - Restricts operations to the private host VM subnet boundary (192.168.56.*).
#   - Enforces specific user context allocation ('vboxuser') to align with host privileges.
#
# IMMUTABLE CHANGE HISTORY (DO NOT DELETE):
#   - ADDED (Remediation Hardening - 2026-06-29):
#     • Created production asset map targeting the Ubuntu 24.04 core VM environment.
#     • Bound explicit host context maps to standard internal bridge adapter networks.
#
# - DO-NOT-DELETE RULE (ABSOLUTE):
#   This IMMUTABLE CHANGE HISTORY section acts as the institutional memory for future AI sessions.
#   It must never be deleted, truncated, rewritten, or regenerated. Future AI must append only.
# ==============================================================================

[treishvaam_production]
treishvaam-node-primary ansible_host=192.168.56.101 ansible_user=vboxuser ansible_port=22

[treishvaam_production:vars]
ansible_connection=ssh
ansible_python_interpreter=/usr/bin/python3
project_root_dir=/opt/treishvaam