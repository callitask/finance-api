# Allow all crawlers
User-agent: *

# Allow access to all content by default
Allow: /

# Disallow crawlers from indexing API, admin, and search paths
Disallow: /api/
Disallow: /dashboard/
Disallow: /blog?q=*

# Point crawlers to the sitemap
Sitemap: https://treishfin.treishvaamgroup.com/sitemap.xml