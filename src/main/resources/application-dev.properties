# ===================================================================
# DEVELOPMENT SETTINGS (dev profile)
# ===================================================================
server.port=8081
app.base-url=http://localhost:3000

# --- DATABASE (DEV) ---
spring.datasource.url=jdbc:mariadb://localhost:3306/finance_db
spring.datasource.username=root
spring.datasource.password=ask123

# --- JPA & LIQUIBASE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.liquibase.enabled=true

# --- JSON SERIALIZATION ---
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss

# --- SECURITY & SECRETS ---
app.security.internal-secret=DEV_SECRET_DO_NOT_USE_IN_PROD_CHANGE_THIS
jwt.secret=your-super-secret-key-that-is-long-and-random
jwt.expiration.ms=86400000

# --- CORS (Allow Localhost Frontend) ---
app.cors.allowed-origins=http://localhost:3000
cors.allowed-origins=http://localhost:3000

# --- FILE STORAGE ---
storage.upload-dir=${user.home}/treishvaam-uploads-dev
storage.sitemap-dir=${user.home}/treishvaam-uploads-dev/sitemaps

# --- MINIO STORAGE (DEV) ---
storage.s3.endpoint=http://localhost:9000
storage.s3.region=us-east-1
storage.s3.access-key=admin
storage.s3.secret-key=treishvaam_storage_secret
storage.s3.bucket=treishvaam-uploads
minio.bucket-name=treishvaam-uploads

# --- ELASTICSEARCH (DEV) ---
spring.elasticsearch.uris=http://localhost:9200
spring.elasticsearch.socket-timeout=30s
spring.elasticsearch.connection-timeout=5s

# --- EXTERNAL APIS (Placeholders for Dev) ---
# You can replace these with real keys if you need to test market data locally
fmp.api.key=demo
alphavantage.api.key=demo
finnhub.api.key=demo
newsdata.api.key=YOUR_DEV_NEWSDATA_API_KEY

# --- GOOGLE ANALYTICS ---
ga4.property-id=dummy_dev_id
ga4.credentials-path=classpath:ga4-credentials-dev.json