# --- DEVELOPMENT (HOST) DATABASE SETTINGS ---
spring.datasource.url=jdbc:mariadb://localhost:3306/finance_db
spring.datasource.username=root
spring.datasource.password=ask123

# --- COMMON SETTINGS ---
# Server Port
server.port=8080

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT Secret Key
jwt.secret=your-super-secret-key-that-is-long-and-random
jwt.expiration.ms=86400000

# File Upload Configuration
file.upload-dir=${user.home}/uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Allowed frontend domains for CORS
app.cors.allowed-origins=http://localhost:3000,https://treishfin.treishvaamgroup.com,https://export.treishvaamgroup.com