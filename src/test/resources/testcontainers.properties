# Force Testcontainers to use the standard Linux Docker socket
docker.client.strategy=org.testcontainers.dockerclient.UnixSocketClientProviderStrategy

# Disable the "Ryuk" container checks if they are causing timing issues (optional, keep enabled usually)
# ryuk.container.privileged=true